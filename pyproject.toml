[project]
name = "cpys"
version = "1.0.0"
authors = [
  { name="Stella Bourdin", email="stella.bourdin@lsce.ipsl.fr" },
]
description = "A python package from compute the Hart Cyclone Phase Space parameters"
requires-python = ">=3.6"
dependencies = [
    "numpy",
    "pandas",
    "xarray",
    "pyproj",
    "matplotlib",
    "scipy",
]

[tool.setuptools]
packages = ["cpys"]

[tool.ruff]
include = ["cpys/**/*.py"]

[tool.ruff.lint]
extend-select = [
    "N", # pep8 naming conventions
]
